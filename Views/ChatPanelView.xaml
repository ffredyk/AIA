<UserControl x:Class="AIA.Views.ChatPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AIA.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Chat Header -->
        <Grid Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ui:Button Margin="5,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Add12" Foreground="White"/>
                    <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Foreground="White" FontWeight="SemiBold" Text="New chat"/>
                </StackPanel>
            </ui:Button>
            <ui:Button Grid.Column="1" Margin="5,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Delete12" Foreground="White"/>
                    <TextBlock Margin="0,0,0,0" VerticalAlignment="Center" Foreground="White" FontWeight="SemiBold" Text=""/>
                </StackPanel>
            </ui:Button>
            <ui:SymbolIcon Grid.Column="2" Margin="25,0,5,0" Symbol="Chat12" Foreground="White" />
            <ComboBox Grid.Column="3" 
                      Height="32" 
                      Foreground="White" 
                      ItemsSource="{Binding ActiveChats}" 
                      SelectedItem="{Binding SelectedChatSession, Mode=TwoWay}"
                      BorderBrush="#7FFFFFFF">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Margin="0,5,0,5" Text="{Binding ChatTitle}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>
        
        <!-- Chat Messages Area -->
        <Border Grid.Row="1" BorderBrush="#19FFFFFF" BorderThickness="1" CornerRadius="8">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#33000000"/>
                    <GradientStop Color="#7F000000" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="10">
                <ItemsControl x:Name="ChatMessagesControl" ItemsSource="{Binding SelectedChatSession.Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,8">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Role}" Value="user">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="assistant">
                                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                
                                <Border MaxWidth="400" CornerRadius="12" Padding="12,8">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}" Value="user">
                                                    <Setter Property="Background" Value="#FF0078D4"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="assistant">
                                                    <Setter Property="Background" Value="#FF2D2D30"/>
                                                    <Setter Property="BorderBrush" Value="#FF3E3E42"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    
                                    <TextBox Text="{Binding Content, Mode=OneWay}" 
                                             Foreground="White" 
                                             Background="Transparent"
                                             BorderThickness="0"
                                             IsReadOnly="True"
                                             TextWrapping="Wrap"
                                             FontSize="13"
                                             Cursor="IBeam"
                                             Padding="0"
                                             VerticalAlignment="Center">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="TextBox">
                                                            <ScrollViewer x:Name="PART_ContentHost" 
                                                                          Focusable="False" 
                                                                          HorizontalScrollBarVisibility="Hidden" 
                                                                          VerticalScrollBarVisibility="Hidden"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        
        <!-- AI Status Bar -->
        <Border Grid.Row="2" Margin="0,8,0,0" Padding="8,6" CornerRadius="6" Background="#19FFFFFF"
                Visibility="{Binding AiStatusMessage, Converter={StaticResource NullToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Processing Indicator -->
                <ui:SymbolIcon Symbol="Bot16" Foreground="#0078D4" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center">
                    <ui:SymbolIcon.Style>
                        <Style TargetType="ui:SymbolIcon">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAiProcessing}" Value="True">
                                    <Setter Property="Symbol" Value="ArrowSync12"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:SymbolIcon.Style>
                </ui:SymbolIcon>
                
                <TextBlock Grid.Column="1" Text="{Binding AiStatusMessage}" Foreground="#999999" FontSize="11" 
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                
                <!-- Provider Count Badge -->
                <Border Grid.Column="2" Padding="6,2" CornerRadius="4" Background="#330078D4" VerticalAlignment="Center">
                    <TextBlock FontSize="10" Foreground="#0078D4">
                        <Run Text="{Binding AIOrchestration.Providers.Count, Mode=OneWay}"/>
                        <Run Text=" providers"/>
                    </TextBlock>
                </Border>
            </Grid>
        </Border>
        
        <!-- Message Input Area -->
        <Border Grid.Row="3" Margin="0,8,0,0" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="8" Background="#19FFFFFF">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="MessageInput"
                         Text="{Binding MessageInput, UpdateSourceTrigger=PropertyChanged}"
                         Background="Transparent"
                         Foreground="White"
                         BorderThickness="0"
                         Padding="12,10"
                         FontSize="14"
                         VerticalAlignment="Center"
                         TextWrapping="Wrap"
                         MaxHeight="100"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         KeyDown="MessageInput_KeyDown"
                         IsEnabled="{Binding IsAiProcessing, Converter={StaticResource InverseBoolToVis}, ConverterParameter=Bool}">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type TextBox}">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                
                <ui:Button Grid.Column="1" 
                           Margin="5"
                           Padding="12,8"
                           Background="#FF0078D4"
                           CornerRadius="6"
                           Click="BtnSendMessage"
                           IsEnabled="{Binding IsAiProcessing, Converter={StaticResource InverseBoolToVis}, ConverterParameter=Bool}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Send16" Foreground="White"/>
                        <TextBlock Margin="6,0,0,0" 
                                   VerticalAlignment="Center" 
                                   Foreground="White" 
                                   FontWeight="SemiBold" 
                                   Text="Send"/>
                    </StackPanel>
                </ui:Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
