<UserControl x:Class="AIA.Views.ChatTemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AIA"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#19FFFFFF" Padding="8,6" CornerRadius="6,6,0,0" Margin="0,0,0,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="ChatSparkle20" Foreground="#0078D4" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{local:Localize Chat_QuickPrompts}" Foreground="White" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
                
                <TextBlock Grid.Column="1" Text="{local:Localize Chat_SelectTemplate}" Foreground="#666666" FontSize="10" 
                           VerticalAlignment="Center" Margin="8,0,0,0" TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>
        
        <!-- Templates List -->
        <Border Grid.Row="1" BorderBrush="#19FFFFFF" BorderThickness="1" CornerRadius="0,0,6,6" Background="#0D000000">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="4">
                <ItemsControl x:Name="TemplatesControl" ItemsSource="{Binding ChatMessageTemplates}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="4" CornerRadius="6" Cursor="Hand"
                                    MouseLeftButtonUp="TemplateButton_Click"
                                    Tag="{Binding}"
                                    ToolTip="{Binding Message}"
                                    Width="180" Height="70"
                                    Background="#19FFFFFF"
                                    BorderBrush="#33FFFFFF"
                                    BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#26FFFFFF"/>
                                                <Setter Property="BorderBrush" Value="#4DFFFFFF"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                
                                <Grid Margin="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Icon -->
                                    <Border Grid.Column="0" Grid.RowSpan="2" Width="36" Height="36" 
                                            CornerRadius="8" Margin="0,0,8,0" VerticalAlignment="Top"
                                            Background="#33FFFFFF">
                                        <ui:SymbolIcon Symbol="{Binding Icon}" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center"
                                                       Foreground="White"/>
                                    </Border>
                                    
                                    <!-- Title -->
                                    <TextBlock Grid.Column="1" Grid.Row="0" 
                                               Text="{Binding Title}" 
                                               Foreground="White" 
                                               FontWeight="SemiBold" 
                                               FontSize="11" 
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Top"/>
                                    
                                    <!-- Description -->
                                    <TextBlock Grid.Column="1" Grid.Row="1" 
                                               Text="{Binding Description}" 
                                               Foreground="#AAAAAA" 
                                               FontSize="9" 
                                               TextWrapping="Wrap"
                                               TextTrimming="CharacterEllipsis"
                                               MaxHeight="36"
                                               VerticalAlignment="Top"
                                               Margin="0,2,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        
        <!-- Empty State -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                    x:Name="EmptyState" Visibility="Collapsed">
            <ui:SymbolIcon Symbol="ChatSparkle20" FontSize="32" Foreground="#666666"/>
            <TextBlock Text="{local:Localize Chat_NoTemplates}" Foreground="#666666" FontSize="11" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
